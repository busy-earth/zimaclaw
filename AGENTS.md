# AGENTS.md

## Cursor Cloud specific instructions

This is a new, empty repository ("zimaclaw — OpenClaw without the hops") initialized with only `README.md` and `LICENSE` (MIT). As of the initial setup, there is:

- No application code, no source directories
- No dependency manifests (`package.json`, `requirements.txt`, etc.)
- No build tooling, linters, or test frameworks
- No Docker, CI/CD, or devcontainer configuration
- No services to start or test

When application code is added, update this section with:
- How to install dependencies
- How to run lint, tests, build, and dev server
- Any non-obvious caveats discovered during setup
